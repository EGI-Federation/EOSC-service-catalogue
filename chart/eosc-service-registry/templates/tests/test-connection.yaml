apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eosc-service-registry.fullname" . }}-test-connection"
  labels:
    {{- include "eosc-service-registry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eosc-service-registry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
